using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Exploding : MonoBehaviour
{

    //Damage made within an area
    private float radialDamage;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void SetRadialDamage()
    {
        //Creates the area of effect explosion and applies the damage to the surrounding area

    }
}
